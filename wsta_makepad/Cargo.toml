[package]
name = "wsta_makepad"
version = "0.1.0"
edition = "2021"

[dependencies]

serde = { version = "1", features = ["derive"] }
serde_json = "1"

makepad-widgets = "1.0.0"

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
