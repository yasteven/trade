// trade/wsta/src/web/embedded_index_html.rs
//
// The old HTML/iframe shell is intentionally retired.
// wsta now serves the generated Makepad package directly at /.
// This constant remains only so older imports do not matter during transition.

pub const INDEX_HTML: &str = "";
